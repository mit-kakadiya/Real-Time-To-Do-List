<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ToDoList" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <!--        <item name="windowActionBarOverlay">true</item>-->
        <!--        <item name="colorOnPrimary">@color/black</item>-->
        <!--        <item name="colorPrimary">@android:color/white</item>-->
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>


    <style name="Theme.ToDoList" parent="Base.Theme.ToDoList" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background, animated icon, and animation
        duration. -->
        <item name="windowSplashScreenBackground">@color/black</item>

        <!-- Use windowSplashScreenAnimatedIcon to add a drawable or an animated
             drawable. One of these is required. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_todo</item>
        <!-- Required for animated icons. -->
        <item name="windowSplashScreenAnimationDuration">5000</item>

        <!-- Set the theme of the Activity that directly follows your splash
        screen. This is required. -->
        <item name="postSplashScreenTheme">@style/Theme.ToDoList</item>
    </style>

    <style name="OutlinedYellowButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="backgroundTint">@null</item>
        <item name="android:background">@drawable/button_outlined_yellow</item>
        <item name="android:textColor">@color/medium</item>
        <item name="fontFamily">@font/poppins_regular</item>
    </style>

    <style name="Style_TextView_Bold">
        <item name="android:fontFamily">@font/poppins_bold</item>
        <item name="android:fontWeight" tools:targetApi="o">700</item>
        <item name="android:clickable">true</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="Style_TextView_Regular">
        <item name="android:fontFamily">@font/poppins_regular</item>
        <item name="android:fontWeight" tools:targetApi="o">500</item>
        <item name="android:clickable">true</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="Style_TextView_Medium">
        <item name="android:fontFamily">@font/poppins_medium</item>
        <item name="android:fontWeight" tools:targetApi="o">400</item>
        <item name="android:clickable">true</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="Style_TextView_SemiBold">
        <item name="android:fontFamily">@font/poppins_semibold</item>
        <item name="android:fontWeight" tools:targetApi="o">600</item>
        <item name="android:clickable">true</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="NoBorderBlackBackgroundTextInputLayout" parent="Widget.Material3.TextInputLayout.FilledBox.Dense.ExposedDropdownMenu">
        <item name="boxBackgroundMode">filled</item>
        <item name="boxBackgroundColor">@color/white</item>
        <item name="boxStrokeWidth">0dp</item>
        <item name="boxStrokeWidthFocused">0dp</item>
        <item name="boxStrokeColor">?attr/colorSurface</item> <!-- Makes the border invisible -->
        <item name="boxStrokeColor" tools:ignore="DuplicateDefinition">@android:color/transparent
        </item> <!-- Ensures no border color -->
        <item name="hintTextColor">@color/black</item>
        <item name="placeholderTextColor">@color/black</item>
        <item name="endIconTint">@color/black</item>
        <item name="hintEnabled">true</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Widget.App.FloatingActionButton" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">20dp</item>
        <item name="strokeWidth">1.5dp</item>
        <item name="elevation">6dp</item>
        <item name="backgroundTint">@color/medium</item>
        <item name="tint">@color/white</item>
    </style>
</resources>